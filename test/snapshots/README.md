# test/snapshots/ — per-profile expected probe outputs (Plan 04)

Regression baselines: one snapshot per profile, captured on a matching-OS host. CI fails on drift
after any Chromium rebase. Snapshots are host-OS/GPU specific (README §5), so each shipped OS keeps
its own set.
