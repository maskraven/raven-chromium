# Python bytecode
__pycache__/
*.py[cod]

# macOS
.DS_Store

# Build / packaging outputs (if anyone builds/pkgs in-tree)
/dist/
/out/

# Editor cruft
*.swp
*~

# Local, machine-specific Windows-on-ARM build driver (not for the repo)
/build/build-windows.ps1
